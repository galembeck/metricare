/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '@/lib/api-client';
import type { GetUsersMeQueryResponse } from '../../types/GetUsersMe.ts';
import type { Client, RequestConfig, ResponseErrorConfig } from '@/lib/api-client';

function getGetUsersMeUrl() {
  const res = { method: 'GET', url: `/users/me` as const };
  return res;
}

/**
 * @description Get the authenticated user's profile
 * @summary /users/me
 * {@link /users/me}
 */
export async function getUsersMe(config: Partial<RequestConfig> & { client?: Client } = {}) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<GetUsersMeQueryResponse, ResponseErrorConfig<Error>, unknown>({
    method: 'GET',
    url: getGetUsersMeUrl().url.toString(),
    ...requestConfig,
  });
  return res.data;
}
