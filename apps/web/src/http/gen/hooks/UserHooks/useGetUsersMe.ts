/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { GetUsersMeQueryResponse } from '../../types/GetUsersMe.ts';
import type { Client, RequestConfig, ResponseErrorConfig } from '@/lib/api-client';
import type { QueryKey, QueryClient, QueryObserverOptions, UseQueryResult } from '@tanstack/react-query';
import { getUsersMe } from '../../clients/UserService/getUsersMe.ts';
import { queryOptions, useQuery } from '@tanstack/react-query';

export const getUsersMeQueryKey = () => [{ url: '/users/me' }] as const;

export type GetUsersMeQueryKey = ReturnType<typeof getUsersMeQueryKey>;

export function getUsersMeQueryOptions(config: Partial<RequestConfig> & { client?: Client } = {}) {
  const queryKey = getUsersMeQueryKey();
  return queryOptions<GetUsersMeQueryResponse, ResponseErrorConfig<Error>, GetUsersMeQueryResponse, typeof queryKey>({
    queryKey,
    queryFn: async ({ signal }) => {
      return getUsersMe({ ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Get the authenticated user's profile
 * @summary /users/me
 * {@link /users/me}
 */
export function useGetUsersMe<
  TData = GetUsersMeQueryResponse,
  TQueryData = GetUsersMeQueryResponse,
  TQueryKey extends QueryKey = GetUsersMeQueryKey,
>(
  options: {
    query?: Partial<
      QueryObserverOptions<GetUsersMeQueryResponse, ResponseErrorConfig<Error>, TData, TQueryData, TQueryKey>
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {}
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? getUsersMeQueryKey();

  const query = useQuery(
    {
      ...getUsersMeQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
