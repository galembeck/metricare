/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { GetUsersMeQueryResponse } from '../../types/GetUsersMe.ts';
import type { Client, RequestConfig, ResponseErrorConfig } from '@/lib/api-client';
import type { QueryKey, QueryClient, UseSuspenseQueryOptions, UseSuspenseQueryResult } from '@tanstack/react-query';
import { getUsersMe } from '../../clients/UserService/getUsersMe.ts';
import { queryOptions, useSuspenseQuery } from '@tanstack/react-query';

export const getUsersMeSuspenseQueryKey = () => [{ url: '/users/me' }] as const;

export type GetUsersMeSuspenseQueryKey = ReturnType<typeof getUsersMeSuspenseQueryKey>;

export function getUsersMeSuspenseQueryOptions(config: Partial<RequestConfig> & { client?: Client } = {}) {
  const queryKey = getUsersMeSuspenseQueryKey();
  return queryOptions<GetUsersMeQueryResponse, ResponseErrorConfig<Error>, GetUsersMeQueryResponse, typeof queryKey>({
    queryKey,
    queryFn: async ({ signal }) => {
      return getUsersMe({ ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Get the authenticated user's profile
 * @summary /users/me
 * {@link /users/me}
 */
export function useGetUsersMeSuspense<
  TData = GetUsersMeQueryResponse,
  TQueryKey extends QueryKey = GetUsersMeSuspenseQueryKey,
>(
  options: {
    query?: Partial<UseSuspenseQueryOptions<GetUsersMeQueryResponse, ResponseErrorConfig<Error>, TData, TQueryKey>> & {
      client?: QueryClient;
    };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {}
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? getUsersMeSuspenseQueryKey();

  const query = useSuspenseQuery(
    {
      ...getUsersMeSuspenseQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as UseSuspenseQueryOptions,
    queryClient
  ) as UseSuspenseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
